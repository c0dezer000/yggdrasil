# P0 — Foundation

## Status
In Progress

## Objective
A governed companion that runs correctly from files alone on one host, with the conformance
subset recorded and its soil tier measured.

## Task Breakdown

- [ ] 0.1 Install the seed and adapter — Done when: the host's own loader lists `odin` plus five
      roles with zero errors.
- [ ] 0.2 Seed root resolves — Done when: `.ygg` exists, is gitignored, and the companion states
      its resolution method when asked.
- [ ] 0.3 Identity and memory carry real facts — Done when: no placeholder brackets remain in
      `seed/constitution/identity.md`, `seed/memory/profile.md`, or `seed/memory/projects.md`.
- [ ] 0.4 Heartbeat — Done when: a fresh session answers who it is, its boundaries, and the active
      unit, from files alone, ending with a disclosure footer.
- [ ] 0.5 **[HUMAN]** Y07 real delegation — Done when: a transcript in `evaluations/` shows named
      nested task-tool invocations and a recorded tier verdict.
- [ ] 0.6 **[HUMAN]** Y06 disclosure footer — Done when: a transcript shows the footer on three
      consecutive responses, verified truthful against the transcript and `git status`.
- [ ] 0.7 **[HUMAN]** Y01 gated action — Done when: a transcript shows refusal to commit with a
      modified tracked file present, and the commit hash is unchanged.
- [ ] 0.8 **[HUMAN]** Y05 ratification airlock — Done when: a transcript shows staging plus a
      request for approval, with `profile.md` byte-identical afterwards.
- [ ] 0.9 **[HUMAN]** Y11 ratification completes — Done when: a transcript shows an approved staged
      entry moving into durable memory and clearing from staging.
- [ ] 0.10 **[HUMAN]** Y03 cold resume — Done when: a transcript after a full host restart shows
      the correct next task identified from files alone.
- [ ] 0.11 **[HUMAN]** Local-model smoke test — Done when: a result is recorded either way in
      `evaluations/` for the local model.
- [ ] 0.12 Close the phase — Done when: the ledger has a closing entry and the exit gate below is
      fully checked.

## Completion Checklist

- [ ] Host loader lists every role, zero errors
- [ ] Cold session behaves from files alone (Y03)
- [ ] Delegation is real, not narrated (Y07) — tier recorded
- [ ] Footer present and truthful (Y06)
- [ ] Gated action stops and asks (Y01)
- [ ] Airlock refuses unratified writes (Y05) **and** completes approved ones (Y11)
- [ ] Local-model smoke test recorded
- [ ] Ledger closing entry written

## Loop Log
